name: PACZero MLX-LM smoke scaffold

on:
  workflow_dispatch:
  push:
    branches: ["main"]
    paths:
      - "scripts/paczero_mlxlm_smoke.py"
      - ".github/workflows/paczero-mlxlm-smoke.yml"

permissions:
  contents: write

defaults:
  run:
    shell: bash

jobs:
  mlxlm-smoke:
    name: Real model adapter plumbing smoke
    runs-on: macos-26
    timeout-minutes: 60
    if: ${{ github.event_name != 'push' || !contains(github.event.head_commit.message, '[skip paczero-mlxlm-smoke]') }}
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
      PYTHONUNBUFFERED: "1"
      PYTHONWARNINGS: "ignore"
      HF_HUB_DISABLE_PROGRESS_BARS: "1"
      HF_XET_HIGH_PERFORMANCE: "1"

    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: true

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install MLX-LM dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install --upgrade mlx mlx-lm huggingface_hub hf_transfer safetensors

      - name: Run model-level MLX-LM smoke scaffold
        run: |
          mkdir -p benchmark-logs/paczero-mlxlm-smoke benchmark-results
          stamp="$(date -u '+%Y%m%dT%H%M%SZ')"
          log_file="benchmark-logs/paczero-mlxlm-smoke/paczero-mlxlm-smoke-${stamp}.txt"
          latest_file="benchmark-logs/paczero-mlxlm-smoke/paczero-mlxlm-smoke-latest.txt"
          result_file="benchmark-results/paczero_mlxlm_smoke_results.json"
          echo "LOG_FILE=$log_file" >> "$GITHUB_ENV"
          echo "LATEST_FILE=$latest_file" >> "$GITHUB_ENV"
          echo "RESULT_FILE=$result_file" >> "$GITHUB_ENV"
          set -o pipefail
          python scripts/paczero_mlxlm_smoke.py \
            --model mlx-community/Qwen2.5-0.5B-Instruct-4bit \
            --iters 2 \
            --batch-size 1 \
            --num-layers 2 \
            --json-out "$result_file" \
            2>&1 | tee "$log_file"
          cp "$log_file" "$latest_file"

      - name: Add summary
        if: always()
        run: |
          echo "# PACZero MLX-LM smoke scaffold" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "Log: \`${LATEST_FILE:-not-created}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "Result JSON: \`${RESULT_FILE:-not-created}\`" >> "$GITHUB_STEP_SUMMARY"
          if [ -f "${LOG_FILE:-}" ]; then
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo '```text' >> "$GITHUB_STEP_SUMMARY"
            tail -n 160 "$LOG_FILE" >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Commit smoke output
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add benchmark-logs/paczero-mlxlm-smoke/ benchmark-results/paczero_mlxlm_smoke_results.json benchmark-results/paczero-mlxlm-smoke/train.log || true
          if git diff --cached --quiet; then
            echo "No PACZero MLX-LM smoke output changes to commit."
          else
            git commit -m "Add PACZero MLX-LM smoke output [skip ci] [skip paczero-mlxlm-smoke]"
            git push
          fi

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: paczero-mlxlm-smoke-results
          path: |
            benchmark-logs/paczero-mlxlm-smoke
            benchmark-results/paczero_mlxlm_smoke_results.json
            benchmark-results/paczero-mlxlm-smoke/train.log
          if-no-files-found: warn
