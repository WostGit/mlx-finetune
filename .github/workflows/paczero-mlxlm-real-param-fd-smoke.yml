name: PACZero MLX-LM real-parameter FD smoke

on:
  workflow_dispatch:
  push:
    branches: ["main"]
    paths:
      - "scripts/paczero_mlxlm_real_param_fd_smoke.py"
      - ".github/workflows/paczero-mlxlm-real-param-fd-smoke.yml"

permissions:
  contents: write

defaults:
  run:
    shell: bash

jobs:
  real-param-fd-smoke:
    name: Real model parameter finite-difference smoke
    runs-on: macos-26
    timeout-minutes: 60
    if: ${{ github.event_name != 'push' || !contains(github.event.head_commit.message, '[skip paczero-real-param-fd]') }}
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
      PYTHONUNBUFFERED: "1"
      PYTHONWARNINGS: "ignore"
      HF_HUB_DISABLE_PROGRESS_BARS: "1"
      HF_XET_HIGH_PERFORMANCE: "1"

    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: true

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install MLX-LM dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install --upgrade mlx mlx-lm huggingface_hub hf_transfer safetensors numpy

      - name: Run real-parameter finite-difference smoke
        run: |
          mkdir -p benchmark-logs/paczero-mlxlm-real-param-fd-smoke benchmark-results
          stamp="$(date -u '+%Y%m%dT%H%M%SZ')"
          log_file="benchmark-logs/paczero-mlxlm-real-param-fd-smoke/paczero-mlxlm-real-param-fd-smoke-${stamp}.txt"
          latest_file="benchmark-logs/paczero-mlxlm-real-param-fd-smoke/paczero-mlxlm-real-param-fd-smoke-latest.txt"
          result_file="benchmark-results/paczero_mlxlm_real_param_fd_smoke_results.json"
          echo "LOG_FILE=$log_file" >> "$GITHUB_ENV"
          echo "LATEST_FILE=$latest_file" >> "$GITHUB_ENV"
          echo "RESULT_FILE=$result_file" >> "$GITHUB_ENV"
          set -o pipefail
          python scripts/paczero_mlxlm_real_param_fd_smoke.py \
            --model mlx-community/Qwen2.5-0.5B-Instruct-4bit \
            --param-path model.layers.0.self_attn.q_proj.bias \
            --mu 0.05 \
            --clip 25.0 \
            --json-out "$result_file" \
            2>&1 | tee "$log_file"
          cp "$log_file" "$latest_file"

      - name: Add summary
        if: always()
        run: |
          echo "# PACZero MLX-LM real-parameter FD smoke" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "Log: \`${LATEST_FILE:-not-created}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "Result JSON: \`${RESULT_FILE:-not-created}\`" >> "$GITHUB_STEP_SUMMARY"
          if [ -f "${LOG_FILE:-}" ]; then
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo '```text' >> "$GITHUB_STEP_SUMMARY"
            tail -n 200 "$LOG_FILE" >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Commit real-parameter smoke output
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add benchmark-logs/paczero-mlxlm-real-param-fd-smoke/ benchmark-results/paczero_mlxlm_real_param_fd_smoke_results.json || true
          if git diff --cached --quiet; then
            echo "No PACZero real-parameter smoke output changes to commit."
          else
            git commit -m "Add PACZero MLX-LM real-parameter FD smoke output [skip ci] [skip paczero-real-param-fd]"
            git push
          fi

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: paczero-mlxlm-real-param-fd-smoke-results
          path: |
            benchmark-logs/paczero-mlxlm-real-param-fd-smoke
            benchmark-results/paczero_mlxlm_real_param_fd_smoke_results.json
          if-no-files-found: warn
